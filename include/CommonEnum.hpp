#ifndef COMMON_ENUM_HPP
#define COMMON_ENUM_HPP

enum RenderType {
	RENDER_ONCE,
	RENDER_LOOP
};

enum DrawType {
	DRAW_TRIANGLES,
	DRAW_LINE,
	DRAW_POINT,
};

enum ProjectionType {
	PROJECTION_PERSPECTIVE,
	PROJECTION_ORTHO
};

enum CenterType {
	CENTER_CAMERA,
	CENTER_OBJECT
};

enum TextureType {
	TEXTURE_DIFFUSE,
	TEXTURE_NORMAL,
	TEXTURE_SPECULAR,
	TEXTURE_ALPHA,
	TEXTURE_LIGHT_MAP,
	TEXTURE_SHADOW_MAP,
	TEXTURE_ENV_MAP,
	TEXTURE_SKYBOX,
	TEXTURE_DISPLACEMENT_MAP,
	TEXTURE_HEIGHT_MAP,
	TEXTURE_RANDER_TARGET,
	TEXTURE_TEXTURE_ARRAY,
	TEXTURE_3D
};

enum UniformBlockBindingIndex {
	UBOIDX_CAMERA
};

#endif